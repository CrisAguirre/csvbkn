PORT=3000
MONGODB_URI=mongodb+srv://<USER>:<PASSWORD>@<CLUSTER>/<DB>?appName=Cluster0
JWT_SECRET=your_super_secret_jwt_key

# Admin unico: spaziovitale.gerencia@gmail.com con hash bcrypt fijo en seed-managed-users.js.
# NO usar ADMIN_EMAIL / ADMIN_PASSWORD / DESIGNER_EMAIL / DESIGNER_PASSWORD
# (eliminados 2026-09-26: permitian accesos alternos). Si existen en Render, borrarlos.
# Solo opcionales para cuentas gestionadas (si no se definen usan defaults del codigo):
# COMERCIAL_PASSWORD, CONTABILIDAD1_PASSWORD, CONTABILIDAD2_PASSWORD, DISENO_PASSWORD
